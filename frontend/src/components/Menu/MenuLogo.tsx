import s from './style.module.scss';

const MenuLogo = () => {
    return (
        <div className={s.main}>
            <div className={s.main__img} />
        </div>
    );
};

export default MenuLogo;
